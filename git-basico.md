```bash
git clone https://github.com/tainatravassos/Ecommerce_Mary_Jackson.git

cd Ecommerce_Mary_Jackson
ls (para listar os arquivos)
git status (para verificar o status dos arquivos)
git add .  (para adicionar todos os arquivos)
OU git add git-basico.md (para adicionar um arquivo específico)
git commit -m "Adicionando arquivo git-basico.md"
git push origin main

```
Commits semânticos
```bash 
git commit -m "feat: Adicionando botão de login"
git commit -m "chore: Atualizando arquivo de dependências"
git commit -m "docs: Adicionando documentação do projeto"
git commit -m "style: Alterando estilo do botão de login"
git commit -m "fix: Corrigindo bug na página de login"
git commit -m "refactor: Refatorando código de autenticação"
git commit -m "test: Adicionando testes de autenticação"
```
